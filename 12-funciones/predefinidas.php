<?php

/*DEBUGGEAR
 * var_dump($var); Devuelve la información de la variable.
 * 
 * FECHAS
 * date('d-m-Y'); Devuelve el día el mes y el año. Acepta diferentes variaciones.Consultar documentación PHP.
 * time(); Imprime la fecha en formato único.
 * 
 * MATEMATICAS
 * sqrt($var); Devuelve la raiz cuadrada de la variable.    Consultar documentación PHP
 * rand($min, $max); Devuelve un número aleatorio. Acepta parámetros opcionales, mínimo y máximo para establecer rango.
 * pi(); Devuelve el número pi.
 * round(num, cifras); Devuelve el número redondeado, opcionalmente se puede establecer el parámetro para el número de cifras.
 * 
 * FUNCIONES GENERALES
 * gettype($var); Devuelve el tipo de la variable. 
 * is_float($var); is_string($var); is_array($var); etc. Comprueba si la variable es de el tipo indicado en la función o no. Devuelve true o false.
 * isset($var); Comprueba si la variable existe. Devuelve true o false.
 * trim($var); Elimina los espacios en blanco antes y despues del string.
 * unset($var); Elimina la variable del codigo.
 * empty($var); Comprueba si la variable está vacía, no existe o es nula.
 * strlen($var); Cuenta los caracteres del string.
 * strpos($string,'busqueda'); Encuentra en un string seleccionado el caracter buscado.
 * strreplace('anterior', 'nuevo', $string); Reemplaza la palabra anterior en el string $string por la palabra nueva.
 * strtolower($string); Convierte el string a minúsculas.
 * strtoupper($string); Convierte el string a mayúsculas.
 * 
 * CONSULTAR LA DOCUMENTACIÓN PHP PARA FUNCIONES.
 */

str_rep

?>
